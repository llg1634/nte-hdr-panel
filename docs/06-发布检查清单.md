# 发布检查清单

## 功能检查

- `py -3 -m py_compile app.py` 通过。
- `run.bat` 能启动本地服务。
- 页面能打开 `http://127.0.0.1:22533`。
- `/api/state` 能读取配置状态。
- 写入前会创建 `_nte_hdr_backups`。
- 写入后 `Engine.ini` 包含 HDR 配置。
- 勾选只读时文件属性变成只读。
- 恢复后回到写入前状态。
- `退出工具` 能关闭后端服务。

## UI 检查

- 浅色主题可读。
- 深色主题可读。
- 桌面宽度布局正常。
- 窄屏布局不横向溢出。
- 控制台没有前端错误。

## 发布检查

- `CHANGELOG.md` 已更新版本。
- `README.md` 和 `README.en.md` 内容同步。
- Release zip 包含 `NTEHDRPanel.exe` 和 `_internal` 目录。
- Release notes 说明修改范围、备份恢复和只读策略。
