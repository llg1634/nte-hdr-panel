# 常见问题

## 这是不是 RTX HDR？

不是。这个工具写入 UE RendererSettings，让游戏走 UE HDR 输出配置。

## 为什么要只读？

本机测试发现，不设只读时游戏启动阶段会重写 `Engine.ini`。设为只读后配置能保留。

## 只读会不会影响恢复？

不会。恢复前工具会先取消当前文件只读，再按 manifest 恢复原文件和原只读状态。

## HUD 开关失败怎么办？

用管理员权限启动：

```text
run_as_admin.bat
```

## 游戏提示黑客工具怎么办？

先关闭这些工具：

```text
Procmon64 / Process Monitor
Process Explorer
x64dbg / x32dbg
Cheat Engine
IDA
Frida
```

本工具不会读取游戏模块，也不会注入游戏进程。

## 怎么确认成功？

优先看 NVIDIA HUD：

```text
DX12 / D3D12
HDR Mode: Custom
```

同时确认 `Engine.ini` 没被游戏重写。
